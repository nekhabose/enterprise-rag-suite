# React dev server port (CRA uses PORT)
PORT=3001

# Preferred explicit backend URL
REACT_APP_API_URL=http://localhost:3000

# Optional fallback if REACT_APP_API_URL is not set
REACT_APP_BACKEND_PORT=3000
